# Gmail Labeler: Python 3.11 + Node.js environment
[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[variables]
PIP_BREAK_SYSTEM_PACKAGES = "1"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3 -X utf8 main.py --mode scheduler"
